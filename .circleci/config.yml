version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:8.12.0
    working_directory: ~/code
    steps:
      - checkout
      - run:
          name: hi
          command: echo "hello world"
workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            branches:
              only: /jt-circleci/
